#7.Матрица 5x4 заполняется вводом с клавиатуры, кроме последних элементов строк.
# Программа должна вычислять сумму введенных элементов каждой строки и записывать
# ее в последнюю ячейку строки.
# В конце следует вывести полученную матрицу.

matrix = []

for i in range(4):
    matrix.append([])
    sum = 0
    for n in range(4):
        item = int(input(f'Введите элемент {i+1} и {n+1} столбца: '))
        sum += item
        matrix[i].append(item)

    matrix[i].append(sum)

for line in matrix:
	for item in line:
		print(f'{item:>4}',end='')
	print()

